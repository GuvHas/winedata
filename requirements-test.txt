# Test toolchain for the Home Assistant integration.
#
# Pinned to Python 3.12: pytest-homeassistant-custom-component pins
# numpy==1.26.0, which ships no cp313 wheel and would need a source build.
pytest-homeassistant-custom-component==0.13.109
beautifulsoup4>=4.12.0
lxml>=5.0.0
respx>=0.20.2
ruff>=0.6.0
tzdata>=2024.1
